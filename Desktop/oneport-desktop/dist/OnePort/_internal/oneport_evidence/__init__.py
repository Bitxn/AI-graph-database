"""
oneport-evidence — compliance evidence from the gate you already run.

Every `op ship` run is an audit event: what was checked, what blocked, when. This
turns that history (the local usage ledger + optionally ingested run reports) into
SOC 2 / ISO 27001 evidence packs — deterministic, sign-off-ready, and generated
entirely on your machine.

Deliberately AI-free: an audit trail should contain facts, not model output.
"""

__version__ = "0.1.0"
